<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">



        <!-- 🔍 검색창 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:background="#EEEEEE"
            android:paddingHorizontal="12dp"
            android:layout_marginBottom="20dp">

            <ImageView
                android:id="@+id/btnBack"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_arrow_back"/>

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_search"
                android:layout_marginStart="12dp"/>

            <EditText
                android:id="@+id/edtSearch"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:hint="검색"
                android:textSize="16sp"
                android:paddingStart="8dp"
                android:background="@android:color/transparent"/>

            <ImageView
                android:id="@+id/btnClear"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_close"/>
        </LinearLayout>



        <!-- 🔵 내가 입력한 검색어 표시 -->
        <TextView
            android:id="@+id/tvSearchedKeyword"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="검색어 표시"
            android:textSize="15sp"
            android:textStyle="bold"
            android:padding="8dp"
            android:background="#DDDDDD"
            android:layout_marginBottom="16dp" />



        <!-- 🔥 검색 결과가 붙을 영역 -->
        <LinearLayout
            android:id="@+id/layoutSearchResult"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- ⚠️ 여기 안에 동적으로 결과가 여러 개 추가됨 -->

        </LinearLayout>





        <!-- 🎯 아래는 “결과 카드 템플릿” (숨겨진 상태) -->
        <!-- 코틀린에서 clone/inflate해서 layoutSearchResult에 붙이면 됨 -->
        <LinearLayout
            android:id="@+id/templateResultItem"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp"
            android:background="#F9F9F9"
            android:elevation="2dp"
            android:visibility="gone"     <!-- 🔥 템플릿이므로 화면에서 숨김 -->
            android:layout_marginBottom="12dp">

            <!-- 제목 -->
            <TextView
                android:id="@+id/tvResultTitle_template"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="제목"
                android:textSize="15sp"
                android:textStyle="bold"
                android:layout_marginBottom="6dp"/>

            <!-- 태그 영역 -->
            <LinearLayout
                android:id="@+id/layoutTags_template"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <!-- 태그들은 Kotlin에서 동적으로 복제해서 붙이는 구조 -->
            </LinearLayout>

        </LinearLayout>

    </LinearLayout>
</ScrollView>
